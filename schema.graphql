type Query {
  process: ProcessQuery
}

type ProcessQuery {
  all: [Process!]!
}

type Mutation {
  process: ProcessMutation
}

type ProcessMutation {
  new: Process!
}

# type Subscription {
#   subscribeToProcess(pid: ID): Process
# }

enum ProcessState {
  RUNNING
  FINISHED
  FAILED
}

type Process {
  id: ID!
  status: ProcessState!
}
