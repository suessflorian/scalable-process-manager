type Query {
  allProcesses: [Process!]!
}

type Mutation {
  newProcess: Process
}

# type Subscription {
#   subscribeToProcess(pid: ID): Process
# }

enum ProcessState {
  RUNNING
  FINISHED
  FAILED
}

type Process {
  id: ID!
  status: ProcessState!
}
